

export default function VideoChat() {
  return (
    <>
    </>
  );
}
//<button onClick={() => updateSteamState(true)}> get user media</button>
//<button onClick={() => updateSteamState(true)}>gry media</button> <button onClick={() => createCall(pc)}>createCall</button> <button onClick={() => answerCall(pc, room)}>answer button</button>
